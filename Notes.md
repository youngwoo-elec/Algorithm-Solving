>### 📅 2026-05-02
- `strlen`: 문자열의 **길이** / <string.h>에 내장
- 문제에서 주는 조건 잘 살피기
- `%f` → float  `%lf` → double  `printf`에선 상관없지만 scanf에선 중요!
- **모듈러 연산**: 나머지 연산은 중간과정에서 행해져도 결과에 차이 x   
    → "(A * B) % M" = "((A % M) * (B % M)) % M"


>### 📅 2026-05-03
// training_6   숫자 제거 배열
- `sprintf(배열, "%d", 데이터);`: 다양한 타입의 데이터를 **문자열로 변환**   
    → 저장할 배열의 크기는 넉넉하게
- `strstr(A,B)`: 문자열 안에서 다른 문자열을 찾는다. / <string.h>   
    → 찾았을 때: 주소를 변환    못 찾았을 때: NULL값 변환


>### 📅 2026-05-05
// training_7   큰 숫자 순서로 인덱스 출력하기
- `bubble sort` 사용했더니 N 값이 클 때 timeout 발생  (O(N^2))
- `quick sort` 사용해서 문제 해결   O(NlogN)